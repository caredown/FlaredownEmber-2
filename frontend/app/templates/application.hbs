{{#application-wrapper}}
  {{partial "application/header"}}

  {{outlet}}

  {{#if hasNoSubdomain}}
    {{partial "application/footer"}}
  {{/if}}

  {{#if session.isAuthenticated}}
    {{bottom-nav}}
  {{/if}}

  {{outlet "modal"}}
{{/application-wrapper}}
