{{#application-wrapper as |config|}}
  {{partial "application/header"}}

  {{outlet}}

  {{#if config.isEmptySubdomain}}
    <footer></footer>
  {{else}}
    {{partial "application/footer"}}
  {{/if}}

  {{#if (and session.isAuthenticated (not config.isEmptySubdomain))}}
    {{bottom-nav}}
  {{/if}}

  {{outlet "modal"}}
{{/application-wrapper}}
